{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": [
        "@playwright/mcp@latest",
        "--user-data-dir",
        "${HOME}/.nucleus/playwright-profile"
      ]
    }
  }
}
